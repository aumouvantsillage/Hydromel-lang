#lang hydromel

import "half_adder.hdrml" as ha

component full_adder
    port a  : in  unsigned(1)
    port b  : in  unsigned(1)
    port ci : in  unsigned(1)
    port s  : out unsigned(1)
    port co : out unsigned(1)
    instance h1 = ha::half_adder
    h1.a = a
    h1.b = b
    instance h2 = ha::half_adder
    h2.a = h1.s
    h2.b = ci
    s = h2.s
    co = h1.c or h2.c
end
